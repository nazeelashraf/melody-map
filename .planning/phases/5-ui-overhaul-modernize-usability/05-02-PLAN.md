---
phase: 05-ui-overhaul-modernize-usability
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - src/components/ConfirmDialog.tsx
  - src/components/ImportDialog.tsx
  - src/components/EmptyState.tsx
  - src/components/ExportButton.tsx
  - src/components/SheetCard.tsx
  - src/components/CompositionCard.tsx
  - src/components/SheetList.tsx
autonomous: true
requirements:
  - VIS-01
must_haves:
  truths:
    - "No inline style={{}} objects remain in library components — all replaced with Tailwind classes"
    - "ConfirmDialog uses shadcn AlertDialog for accessibility"
    - "ImportDialog uses shadcn Dialog for accessibility"
    - "SheetCard and CompositionCard display with Lucide icons and Tailwind styling"
    - "SheetList library page renders with responsive grid layout"
    - "EmptyState displays with Tailwind styling and Lucide icon"
    - "ExportButton uses Lucide Download icon"
    - "Dark mode works correctly on all library components"
  artifacts:
    - path: "src/components/ConfirmDialog.tsx"
      provides: "Delete confirmation dialog using shadcn AlertDialog"
      contains: "AlertDialog"
    - path: "src/components/ImportDialog.tsx"
      provides: "File import dialog using shadcn Dialog"
      contains: "DialogContent"
    - path: "src/components/SheetCard.tsx"
      provides: "Sheet preview card with Tailwind styling"
      contains: "className="
    - path: "src/components/CompositionCard.tsx"
      provides: "Composition preview card with Tailwind styling"
      contains: "className="
    - path: "src/components/SheetList.tsx"
      provides: "Library home with responsive grid and Tailwind styling"
      contains: "className="
    - path: "src/components/EmptyState.tsx"
      provides: "Empty state with Tailwind and Lucide icon"
      contains: "Inbox"
    - path: "src/components/ExportButton.tsx"
      provides: "Export button with Lucide icon"
      contains: "Download"
  key_links:
    - from: "src/components/SheetCard.tsx"
      to: "src/components/ConfirmDialog.tsx"
      via: "Delete confirmation flow"
      pattern: "ConfirmDialog"
    - from: "src/components/CompositionCard.tsx"
      to: "src/components/ConfirmDialog.tsx"
      via: "Delete confirmation flow"
      pattern: "ConfirmDialog"
    - from: "src/components/SheetList.tsx"
      to: "src/components/ImportDialog.tsx"
      via: "Import button click"
      pattern: "ImportDialog"
    - from: "src/components/SheetList.tsx"
      to: "src/components/EmptyState.tsx"
      via: "Empty list placeholder"
      pattern: "EmptyState"
---

<objective>
Migrate all library/home components from inline styles to Tailwind CSS + shadcn/ui primitives. Replace ConfirmDialog with shadcn AlertDialog, ImportDialog with shadcn Dialog, and restyle all cards and lists with Tailwind + Lucide icons.

Purpose: The library page (SheetList, SheetCard, CompositionCard) is the app's home screen — it must look modern and professional. These are also the smaller components that can be migrated quickly, clearing the way for the complex editor components in Plan 03.
Output: All 7 library components fully migrated to Tailwind + shadcn with dark mode support.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/5-ui-overhaul-modernize-usability/05-CONTEXT.md
@.planning/phases/5-ui-overhaul-modernize-usability/05-RESEARCH.md

<!-- Prior plan output for continuity -->
@.planning/phases/5-ui-overhaul-modernize-usability/05-01-SUMMARY.md

<interfaces>
<!-- shadcn/ui components installed in Plan 01 -->
From src/components/ui/button.tsx:
```typescript
// shadcn Button with variants: default, destructive, outline, secondary, ghost, link
// Usage: <Button variant="outline" size="sm">
```

From src/components/ui/dialog.tsx:
```typescript
// Dialog, DialogTrigger, DialogContent, DialogHeader, DialogTitle, DialogDescription, DialogFooter, DialogClose
```

From src/components/ui/alert-dialog.tsx:
```typescript
// AlertDialog, AlertDialogTrigger, AlertDialogContent, AlertDialogHeader, AlertDialogTitle, AlertDialogDescription, AlertDialogFooter, AlertDialogCancel, AlertDialogAction
```

From src/components/ui/badge.tsx:
```typescript
// Badge with variants: default, secondary, destructive, outline
```

From src/lib/utils.ts:
```typescript
export function cn(...inputs: ClassValue[]): string;
```

From Lucide React (available icons for this plan):
```typescript
import { Download, Upload, Plus, Music, Layers, Pencil, Trash2, Inbox, ArrowRight, Library } from 'lucide-react';
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace ConfirmDialog + ImportDialog with shadcn; restyle ExportButton + EmptyState</name>
  <files>src/components/ConfirmDialog.tsx, src/components/ImportDialog.tsx, src/components/ExportButton.tsx, src/components/EmptyState.tsx</files>
  <read_first>
    - src/components/ConfirmDialog.tsx (current overlay + card + buttons)
    - src/components/ImportDialog.tsx (current overlay + card + file input)
    - src/components/ExportButton.tsx (current button with Unicode arrow)
    - src/components/EmptyState.tsx (current dashed border + text)
    - src/components/ui/alert-dialog.tsx (shadcn AlertDialog API)
    - src/components/ui/dialog.tsx (shadcn Dialog API)
  </read_first>
  <action>
    1. Replace `src/components/ConfirmDialog.tsx` with shadcn AlertDialog per D-02:
       - Keep the same props interface: `{ message: string; confirmLabel?: string; onConfirm: () => void; onCancel: () => void }`
       - Replace overlay + card with `<AlertDialog open>` wrapping `<AlertDialogContent>`
       - Message goes in `<AlertDialogTitle>`
       - Buttons: `<AlertDialogCancel>` for Cancel, `<AlertDialogAction>` for confirm (with destructive variant styling)
       - Remove ALL inline `style={{}}` — use Tailwind classes exclusively
       - The component is controlled (always rendered when open, parent manages show/hide state)

    2. Replace `src/components/ImportDialog.tsx` with shadcn Dialog per D-02:
       - Keep the same props interface: `{ mode: 'sheet' | 'composition'; onClose: () => void }`
       - Keep the same file reading + Zod validation logic — this is business logic, not UI
       - Replace overlay + card with `<Dialog open onOpenChange={open => !open && onClose()}>` wrapping `<DialogContent>`
       - Title: "Import Sheet" or "Import Composition" in `<DialogTitle>`
       - File button: shadcn Button with Upload icon from Lucide
       - Error display: `text-destructive` Tailwind class
       - Cancel: DialogClose as Button variant="outline"
       - Remove ALL inline `style={{}}`

    3. Restyle `src/components/ExportButton.tsx` per D-01, D-03:
       - Replace button with shadcn Button `variant="outline" size="sm"`
       - Replace Unicode arrow with Lucide `<Download className="h-4 w-4 mr-1.5" />` icon
       - Label text stays the same
       - Remove the `buttonStyle` constant and all `style={{}}`

    4. Restyle `src/components/EmptyState.tsx` per D-01, D-03:
       - Outer div: `flex flex-col items-center justify-center rounded-xl border-2 border-dashed border-border bg-muted/50 p-8 text-center`
       - Icon: Lucide `<Inbox className="h-10 w-10 text-muted-foreground/50 mb-3" />`
       - Title: `text-lg font-semibold text-muted-foreground`
       - Description: `text-sm text-muted-foreground mt-2`
       - Action button: shadcn Button with variant based on `variant` prop ('primary' maps to `variant="default"`, 'secondary' maps to `variant="secondary"`)
       - Remove ALL inline `style={{}}`

    Per D-01: All inline styles replaced with Tailwind. Per D-02: shadcn Dialog/AlertDialog replace custom overlays. Per D-03: Lucide icons replace Unicode symbols.
  </action>
  <verify>
    <automated>npm run typecheck && npm run build</automated>
  </verify>
  <done>
    - ConfirmDialog.tsx uses AlertDialogContent, AlertDialogTitle, AlertDialogAction, AlertDialogCancel — no inline styles
    - ImportDialog.tsx uses DialogContent, DialogTitle, DialogClose — no inline styles, Zod validation logic preserved
    - ExportButton.tsx uses shadcn Button variant="outline" and Lucide Download icon — no inline styles
    - EmptyState.tsx uses Tailwind classes and Lucide Inbox icon — no inline styles
    - grep -c "style={{" returns 0 for all 4 files
  </done>
</task>

<task type="auto">
  <name>Task 2: Restyle SheetCard + CompositionCard with Tailwind + Lucide; migrate SheetList</name>
  <files>src/components/SheetCard.tsx, src/components/CompositionCard.tsx, src/components/SheetList.tsx</files>
  <read_first>
    - src/components/SheetCard.tsx (current card with rename + delete + link)
    - src/components/CompositionCard.tsx (current card with rename + delete + link)
    - src/components/SheetList.tsx (current library home with create form + grid)
    - src/components/ConfirmDialog.tsx (just migrated to shadcn — new import paths)
    - src/components/ImportDialog.tsx (just migrated to shadcn — new import paths)
  </read_first>
  <action>
    1. Restyle `src/components/SheetCard.tsx` per D-01, D-03:
       - Outer card div: `rounded-lg border border-border bg-card p-4 shadow-sm hover:shadow-md transition-shadow`
       - Header row: `flex items-center justify-between gap-3 mb-2`
       - Title: when not editing — `text-base font-semibold cursor-pointer flex-1` with Pencil icon hint on hover; when editing — shadcn Input `size="sm"`
       - "Open" link: shadcn Button `variant="ghost" size="sm"` with ArrowRight icon from Lucide (replaces "Open →" text)
       - Metadata (tempo, lyric lines): `text-sm text-muted-foreground mb-2` — use Music icon from Lucide before tempo
       - Delete button: shadcn Button `variant="ghost" size="sm"` with Trash2 icon from Lucide (replaces text "Delete")
       - ConfirmDialog usage stays the same (props unchanged, just restyled)
       - Remove ALL inline `style={{}}`

    2. Restyle `src/components/CompositionCard.tsx` per D-01, D-03:
       - Same card structure as SheetCard but with Composition-specific content
       - Sheet count: Badge component `variant="secondary"` showing count
       - "Open" link: shadcn Button with ArrowRight icon
       - Delete: shadcn Button with Trash2 icon
       - Remove ALL inline `style={{}}`

    3. Restyle `src/components/SheetList.tsx` per D-01, D-03, D-13:
       - Remove outer `padding: 2rem` and `maxWidth: 1120px` — AppLayout handles padding now
       - Header section: "Song Library" title as `text-2xl font-bold` with Lucide Library icon
       - Description: `text-muted-foreground mt-1`
       - Action buttons row: shadcn Buttons with Lucide icons:
         - "Import Composition": Button `variant="outline"` with Upload icon
         - "Import Sheet": Button `variant="outline"` with Upload icon
         - "+ New Composition": Button `variant="secondary"` with Plus icon
         - "+ New Sheet": Button `variant="default"` with Plus icon
       - Create form: shadcn Input + Button row in a `rounded-lg border bg-card p-4` container
       - Compositions section heading: Layers icon + Badge count
       - Sheets section heading: Music icon + Badge count
       - Grid layout: `grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4 gap-4` per D-13 (responsive)
       - EmptyState and ImportDialog usage stays the same (props unchanged, just restyled)
       - Remove ALL inline `style={{}}`

    Per D-01: All inline styles gone. Per D-03: Lucide icons for all actions. Per D-13: Responsive grid for cards.
  </action>
  <verify>
    <automated>npm run typecheck && npm run build</automated>
  </verify>
  <done>
    - SheetCard.tsx uses Tailwind classes, shadcn Button, Lucide icons — no inline styles
    - CompositionCard.tsx uses Tailwind classes, shadcn Button/Badge, Lucide icons — no inline styles
    - SheetList.tsx uses Tailwind responsive grid, shadcn Buttons, Lucide icons — no inline styles
    - grep -c "style={{" returns 0 for all 3 files
    - Dark mode: all library components render correctly in both light and dark themes
    - Responsive: card grid adjusts from 1 column on mobile to 4 on desktop
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| File input → ImportDialog | User-selected .json file parsed and validated with Zod before dispatching |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-05-03 | T | ImportDialog | mitigate | Zod safeParse validates ALL imported JSON before dispatching to context — prevents malformed data from corrupting state (existing mitigation from Phase 4, preserved) |
| T-05-04 | I | SheetCard/CompositionCard | accept | Display-only cards with no injection vectors — React auto-escapes text content |
</threat_model>

<verification>
1. `npm run typecheck` passes
2. `npm run build` succeeds
3. No `style={{}}` in any of the 7 migrated component files
4. Library page loads with Tailwind-styled cards in responsive grid
5. Create sheet/composition works with restyled form
6. Rename sheet/composition by clicking title works
7. Delete sheet/composition shows shadcn AlertDialog confirmation
8. Import sheet/composition shows shadcn Dialog with file picker
9. Export button shows Download icon
10. Empty states show Inbox icon and Tailwind styling
11. Dark mode works correctly on all library components
12. Responsive: card grid adjusts columns per breakpoint
</verification>

<success_criteria>
- All 7 library components (ConfirmDialog, ImportDialog, EmptyState, ExportButton, SheetCard, CompositionCard, SheetList) fully migrated from inline styles to Tailwind + shadcn
- No `style={{}}` patterns remain in any of these files
- shadcn AlertDialog replaces custom ConfirmDialog overlay
- shadcn Dialog replaces custom ImportDialog overlay
- All Lucide icons in place (Download, Upload, Plus, Music, Layers, Pencil, Trash2, Inbox, ArrowRight, Library)
- Responsive grid for card layout (1→4 columns across breakpoints)
- Dark mode renders correctly on all migrated components
- All existing functionality preserved (CRUD, import/export, navigation)
</success_criteria>

<output>
After completion, create `.planning/phases/5-ui-overhaul-modernize-usability/05-02-SUMMARY.md`
</output>
