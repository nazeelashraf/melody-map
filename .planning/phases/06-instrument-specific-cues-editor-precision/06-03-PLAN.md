---
phase: "06-instrument-specific-cues-editor-precision"
plan: "03"
type: "execute"
wave: 3
depends_on: ["06-02"]
files_modified:
  - "src/components/PerformanceView.tsx"
autonomous: true
requirements:
  - "PERF-01"
  - "PERF-02"
  - "PERF-03"
must_haves:
  truths:
    - "Performance view shows cues for the selected instrument only (not shared)"
    - "Piano and guitar cues render in distinct monospace style above lyrics"
    - "Percussion renders as five aligned labeled lanes (C, H, R, S, B) with legend"
  artifacts:
    - path: "src/components/PerformanceView.tsx"
      provides: "Instrument-specific cue rendering with distinct typography and stacked percussion lanes"
      contains: "Tabs with instrument selection, piano/guitar cue rendering, percussion lane rendering, drum lane legend"
  key_links:
    - from: "src/components/PerformanceView.tsx"
      to: "src/types/index.ts"
      via: "LyricsLine.cues.piano/guitar/drums accessed per instrument selection"
      pattern: "line\\.cues\\[(instrument|piano|guitar|drums)\\]"
    - from: "src/components/PerformanceView.tsx"
      to: "src/lib/lyrics-utils.ts"
      via: "normalizeCueLine, normalizeDrumCues, formatPreviewLine called for rendering"
      pattern: "normalizeCueLine|normalizeDrumCues|formatPreviewLine"
---

<objective>
Update PerformanceView to render instrument-specific cues for the selected instrument only (PERF-01), with distinct monospace styling for piano/guitar (PERF-02) and five labeled stacked lanes for percussion (PERF-03).

Purpose: In performance mode, each player should see only their instrument's cues — piano and guitar see their own independent cue lines rendered above lyrics, and percussion players see five aligned lanes with a compact legend.
Output: Updated PerformanceView with instrument-switched cue rendering and percussion lane display.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-instrument-specific-cues-editor-precision/06-SPEC.md
@src/components/PerformanceView.tsx
@src/lib/lyrics-utils.ts

## Existing PerformanceView Implementation

The current PerformanceView.tsx already partially implements the Phase 6 requirements:

1. **Instrument tabs** (lines 32-40): Tabs for piano, guitar, drums — satisfies PERF-01 structure
2. **Instrument-specific rendering via formatPreviewLine** (lines 84-98): Piano/guitar use `formatPreviewLine(line, instrument)` which accesses `line.cues[instrument]` — satisfies PERF-01 (different content per instrument)
3. **Percussion lanes** (lines 46-82): Uses `normalizeDrumCues` and renders 5 lanes with `drumLaneOrder.map` — satisfies PERF-03
4. **Drum lane legend** (lines 48-53): Shows C crash, H hi-hat, R ride, S snare, B bass — satisfies D-05 (compact lane letters with visible legend)

## What to Verify

PERF-01: "With different cue content stored per instrument, switching instrument changes displayed cues"
- Current code at lines 84-98 uses `formatPreviewLine(line, instrument)` which accesses `line.cues[instrument]` (e.g., `line.cues.piano` for piano tab, `line.cues.guitar` for guitar tab)
- Since `LyricsLine` now stores separate `piano` and `guitar` strings (per CUE-01), this SHOULD work correctly
- Verify that switching tabs actually changes the displayed content (the data layer changed from shared to independent)

PERF-02: "Piano/guitar cues render in distinct monospace style separated from lyrics"
- Current code at line 84: `<pre className="rounded-xl bg-muted/35 p-6 text-xl font-mono...">`
- `formatPreviewLine` at lyrics-utils.ts line 213-217 returns: `${normalizedCueLine}\n${line.lyrics}`
- The cue line is above the lyric line with a newline separator — visually separated
- Font is monospace — distinct styling ✓

PERF-03: "Percussion renders as five aligned labeled lanes"
- Current code at lines 67-72: renders `drumLaneOrder.map` (C, H, R, S, B) with `{lane} {normalizedDrums[lane]}` and then the lyrics
- Uses `normalizedDrumCues` to ensure each lane is same length as lyrics
- Lane labels use compact single letters — satisfies D-05

## Implementation Decisions from 06-CONTEXT.md

- **D-05**: Performance view uses compact lane letters (C, H, R, S, B) with visible legend — currently implemented at lines 48-53
- **D-12 (agent discretion)**: Exact legend placement — currently at top of percussion tab

<interfaces>
<!-- Key existing interfaces from PerformanceView.tsx -->

From src/components/PerformanceView.tsx:
```typescript
interface PerformanceViewProps {
  sheet: Sheet;
}

// Uses instrumentTypes from schemas/sheet.schema.ts
// instrumentLabels: Record<InstrumentType, string> = { piano: 'Piano', guitar: 'Guitar', drums: 'Percussion' }

// Percussion legend at lines 48-53:
// {drumLaneOrder.map((lane) => (
//   <span key={lane} className="rounded-full bg-muted px-2.5 py-1 font-medium">
//     {lane} {drumLaneLabels[lane]}
//   </span>
// ))}

// Piano/guitar rendering at lines 84-98 uses formatPreviewLine
// Percussion lanes at lines 67-72 render each lane with label
```

From src/lib/lyrics-utils.ts:
```typescript
export function formatPreviewLine(line: LyricsLine, instrument: Exclude<InstrumentType, 'drums'>): string;
// Returns: `${normalizeCueLine(line.cues[instrument], line.lyrics.length)}\n${line.lyrics}`

export function normalizeDrumCues(rawValue: Partial<DrumCues>, targetLength: number): DrumCues;

export const drumLaneLabels: Record<DrumLane, string> = {
  C: 'Crash', H: 'Hi-hat', R: 'Ride', S: 'Snare', B: 'Bass'
};
```
</interfaces>

</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify instrument-specific cue switching in PerformanceView (PERF-01)</name>
  <files>src/components/PerformanceView.tsx</files>
  <action>
Read PerformanceView.tsx and verify that switching between piano, guitar, and drums tabs actually renders DIFFERENT cue content.

For piano/guitar (lines 84-98):
- Uses `formatPreviewLine(line, instrument)` which accesses `line.cues[instrument]`
- Since LyricsLine now has `cues.piano` and `cues.guitar` as separate strings (CUE-01), switching tabs will show different content ✓

For drums (lines 46-82):
- Uses `normalizeDrumCues(line.cues.drums, line.lyrics.length)` which accesses `line.cues.drums` (the DrumCues object)
- Each of the 5 lanes is independent, so they can show different markers at same position ✓

Verify that the instrument parameter passed to `formatPreviewLine` at line 96 is the same instrument as the selected tab. The current code at line 94 passes `instrument` to `formatPreviewLine`, so when the piano tab is active, it accesses `line.cues.piano`, and when guitar tab is active, it accesses `line.cues.guitar`.

If correct, add a comment:
`// Phase 6: PERF-01 verified — instrument tabs show instrument-specific cue data (piano/guitar use line.cues[instrument])`

If not correct, fix to ensure instrument-specific cue switching works.
</action>
  <verify>npm run typecheck passes; verify that formatPreviewLine is called with the correct instrument matching the selected tab</verify>
  <done>Performance view shows different cue content when switching between piano, guitar, and percussion tabs</done>
</task>

<task type="auto">
  <name>Task 2: Verify distinct monospace cue styling for piano/guitar (PERF-02)</name>
  <files>src/components/PerformanceView.tsx</files>
  <action>
Read PerformanceView.tsx lines 84-98 and verify PERF-02:

PERF-02 requirement: "Piano/guitar cues render in a distinct monospace style that is visually separated from lyrics"

Current implementation:
- `<pre className="rounded-xl bg-muted/35 p-6 text-xl font-mono leading-relaxed whitespace-pre-wrap...">`
- `formatPreviewLine` returns `${cueLine}\n${lyrics}` — cue is on its own line ABOVE the lyrics

This means:
1. Cue is visually above lyrics (separated by newline) ✓
2. Monospace font (`font-mono`) ✓
3. Larger text size (`text-xl`) makes cues readable ✓
4. Distinct background (`bg-muted/35`) ✓

Verify that `formatPreviewLine` at lyrics-utils.ts line 213-217 correctly formats as cue-first then lyrics:
```typescript
return `${normalizeCueLine(line.cues[instrument], line.lyrics.length)}\n${line.lyrics}`;
```

If correct, add a comment near the piano/guitar rendering section:
`// Phase 6: PERF-02 verified — cue line rendered above lyrics in distinct monospace style`
</action>
  <verify>npm run typecheck passes; verify cue line is visually above and stylistically distinct from lyric line</verify>
  <done>Piano and guitar cues render in distinct monospace style above lyrics</done>
</task>

<task type="auto">
  <name>Task 3: Verify percussion renders as five aligned labeled lanes (PERF-03 and D-05)</name>
  <files>src/components/PerformanceView.tsx</files>
  <action>
Read PerformanceView.tsx lines 46-82 and verify PERF-03 and D-05:

PERF-03 requirement: "Percussion renders as five aligned labeled lanes (C, H, R, S, B) aligned to the lyric line"

D-05 requirement: "Performance view uses compact lane letters (C, H, R, S, B) with visible legend"

Current implementation:
1. Legend (lines 48-53): Shows all 5 lane labels with compact letters: `C crash, H hi-hat, R ride, S snare, B bass`
2. Lane rendering (lines 56-81):
   - `<div className="font-mono text-lg leading-relaxed min-w-max space-y-3">`
   - For each line, renders `drumLaneOrder.map` (C, H, R, S, B) with:
     - `<span className="inline-block w-6 text-primary/80">{lane}</span>` (compact label)
     - `{normalizedDrums[lane]}` (the cue content for that lane)
   - Then renders lyrics with: `<span className="inline-block w-6 text-transparent">_</span>` followed by lyrics

Verify:
- `normalizeDrumCues` at line 63 ensures all 5 lanes are same length as lyrics (alignment)
- Lane labels use single letters (C, H, R, S, B) with legend explaining — satisfies D-05
- Each lane rendered on its own line — satisfies PERF-03
- Aligned to lyrics via `min-w-max` and consistent spacing — satisfies alignment requirement

If correct, add a comment:
`// Phase 6: PERF-03 verified — percussion renders as 5 labeled lanes with compact letters and visible legend (D-05)`
</action>
  <verify>npm run typecheck passes; verify all 5 drum lanes render at same character positions as lyrics</verify>
  <done>Percussion renders as five aligned labeled lanes with visible legend, each lane aligned to lyric text</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| {SheetEditor → PerformanceView} | Sheet data crosses here via props |
| {user → PerformanceView} | Tab selection crosses here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-05 | Denial | Large sheet with many lines | accept | Rendering is synchronous but sheets are self-contained; no external service dependency |
</threat_model>

<verification>
- `npm run typecheck` passes
- `npm run build` succeeds
- Piano/guitar tabs show instrument-specific cue content (different per instrument)
- Cue lines visually distinct and separated from lyrics
- Percussion shows 5 labeled lanes with legend
</verification>

<success_criteria>
- Switching between piano and guitar tabs shows different cue data (PERF-01)
- Piano/guitar cues render in monospace above lyrics with distinct styling (PERF-02)
- Percussion renders as 5 aligned lanes with compact labels and visible legend (PERF-03 and D-05)
- All cue strings aligned to lyric character positions
</success_criteria>

<output>
After completion, create `.planning/phases/06-instrument-specific-cues-editor-precision/06-03-SUMMARY.md`
</output>