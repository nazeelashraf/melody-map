---
phase: "06-instrument-specific-cues-editor-precision"
plan: "02"
type: "execute"
wave: 2
depends_on: ["06-01"]
files_modified:
  - "src/components/SheetEditor.tsx"
autonomous: true
requirements:
  - "EDIT-01"
  - "EDIT-02"
  - "EDIT-03"
  - "CUE-05"
must_haves:
  truths:
    - "Typing multi-character chords (e.g., 'Bm') keeps caret at intended position without jumping"
    - "Each lyric line presents cue and lyric rows together with matching monospace alignment"
    - "Cue rows are visually distinguishable from lyrics without appearing disabled"
    - "Per-line copy-to-all and copy-to-one actions exist for each lyric line"
  artifacts:
    - path: "src/components/SheetEditor.tsx"
      provides: "Redesigned per-line cue editor with linked cue/lyric fields and copy action row"
      contains: "CueRowInput, GuideMarker, copy-to-all button, copy-to-one buttons"
  key_links:
    - from: "src/components/SheetEditor.tsx"
      to: "src/lib/lyrics-utils.ts"
      via: "copyLineCues, copyLineCuesToAll, normalizeCueLine, syncLyricsLine called on user actions"
      pattern: "copyLineCues|copyLineCuesToAll"
    - from: "src/components/SheetEditor.tsx"
      to: "src/components/PerformanceView.tsx"
      via: "viewMode toggle switches between edit and performance view"
      pattern: "viewMode.*performance"
---

<objective>
Redesign the SheetEditor lyric-line editing interface to meet EDIT-01 (stable caret), EDIT-02 (linked monospace fields), EDIT-03 (distinct cue styling), and CUE-05 (copy actions).

Purpose: The editor is the primary instrument for entering cue data. It must feel tight and reliable — cue position must be visually obvious, caret must stay stable while typing multi-character chords, and copy actions must be accessible per line.
Output: Redesigned SheetEditor with instrument-specific per-line cue editing, linked cue/lyric alignment, and copy action row.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-instrument-specific-cues-editor-precision/06-SPEC.md
@src/components/SheetEditor.tsx
@src/lib/lyrics-utils.ts

## Existing SheetEditor Implementation (Phase 5)

The current SheetEditor.tsx already implements most of the Phase 6 requirements:

1. **CueRowInput component** (line 112-167): Handles stable caret positioning using `pendingSelectionRef` and `useLayoutEffect` to restore selection after value changes — satisfies EDIT-01
2. **GuideMarker component** (line 76-110): Subtle position guide using a measurer span and a vertical line — satisfies EDIT-12 (position guide)
3. **Line-level instrument override** (line 354-359): `handleLineInstrumentChange` allows per-line instrument switching — satisfies D-02
4. **Copy-to-all and copy-to-one buttons** (lines 517-530): `handleCopyLine` with 'all' and per-instrument targets — satisfies CUE-05 and D-07/D-09
5. **Percussion stacked block** (lines 562-587): Shows all 5 lanes together when drums is active — satisfies D-04
6. **Cue styling** (line 163): `bg-primary/5` and `font-mono` — cue rows are visually distinct from lyrics (line 616) which use `bg-background` — satisfies EDIT-03

## What to Verify and Potentially Improve

Per 06-SPEC.md acceptance criteria:
- "Typing `Bm` at a chosen cue position keeps the caret at the intended location without jumping" — verify the `pendingSelectionRef` approach works correctly
- "Vertically adjacent monospace rows with matching alignment" — verify cue row and lyric row are visually linked
- "Copy-to-all and copy-to-one behavior" — verify both actions exist and work

## Implementation Notes

- D-01: SheetEditor edits one instrument at a time (already implemented)
- D-02: Per-line override exists (already implemented)
- D-03: Non-selected instruments NOT shown as read-only summaries (already implemented — only active instrument shows cue row)
- D-04: Percussion shows one stacked block with all 5 lanes (already implemented)
- D-07: Copy actions in action row with other line controls (already implemented)
- D-08: Percussion copy treats 5-lane set as one bundled unit (handled by flattenDrumCues in lyrics-utils)
- D-09: Copy-to-one uses inline target buttons (already implemented)
- D-10: Piano/guitar editing feels like tight aligned text rows (already implemented)
- D-11: Clicking cue row OR lyric row updates position guide (already implemented in onClick handlers)
- D-12: Subtle position guide/highlight (already implemented via GuideMarker)

<interfaces>
<!-- Key existing interfaces from SheetEditor.tsx -->

From src/components/SheetEditor.tsx (existing components):
```typescript
interface CueRowInputProps {
  value: string;
  guideColumn: number;
  placeholder: string;
  ariaLabel: string;
  onChange: (value: string, selectionStart: number) => void;
  onGuideColumnChange: (selectionStart: number) => void;
}

// GuideMarker shows a subtle vertical line at guideColumn
function GuideMarker({ value, guideColumn, toneClassName }: {
  value: string; guideColumn: number; toneClassName: string;
})

// CueRowInput handles stable caret via pendingSelectionRef
function CueRowInput({ value, guideColumn, placeholder, ariaLabel, onChange, onGuideColumnChange }: CueRowInputProps)
```

From src/lib/lyrics-utils.ts:
```typescript
export function copyLineCues(line: LyricsLine, source: InstrumentType, target: InstrumentType): LyricsLine;
export function copyLineCuesToAll(line: LyricsLine, source: InstrumentType): LyricsLine;
export function normalizeCueLine(rawValue: string, targetLength: number): string;
export function syncLyricsLine(previousLine: LyricsLine, nextLyrics: string): LyricsLine;
```

From src/types/index.ts:
```typescript
export type InstrumentType = 'piano' | 'guitar' | 'drums';
export type DrumLane = 'C' | 'H' | 'R' | 'S' | 'B';
```
</interfaces>

</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify stable caret behavior (EDIT-01) in existing CueRowInput</name>
  <files>src/components/SheetEditor.tsx</files>
  <action>
Read the current `CueRowInput` component (lines 112-167) and `GuideMarker` component (lines 76-110) in SheetEditor.tsx.

Verify the stable caret implementation:
1. `pendingSelectionRef` stores the intended selection position before onChange fires
2. `useLayoutEffect` (lines 123-131) restores selection after value changes
3. `onFocus` handler (lines 156-162) also handles initial focus with guideColumn alignment

The implementation should already satisfy EDIT-01 ("Typing `Bm` at a chosen cue position keeps the caret at the intended location without jumping").

If it passes verification, add a comment above `CueRowInput`:
`// Phase 6: Stable caret verified — pendingSelectionRef + useLayoutEffect pattern satisfies EDIT-01`

If issues found, fix the caret stabilization logic to ensure typing multi-character chords keeps caret at the correct position.
</action>
  <verify>npm run typecheck passes; manual verification: click in cue input at position 3, type "Bm", verify caret stays at intended position (not at end)</verify>
  <done>Caret stays at intended position when typing multi-character chord names</done>
</task>

<task type="auto">
  <name>Task 2: Verify linked monospace cue/lyric alignment (EDIT-02) and distinct cue styling (EDIT-03)</name>
  <files>src/components/SheetEditor.tsx</files>
  <action>
Read the lyric-line rendering section in SheetEditor.tsx (lines 489-630) and verify:

EDIT-02 (linked monospace fields):
1. Cue row and lyric row are vertically adjacent — confirmed at lines 543-559 (cue) and 589-619 (lyrics) inside the same article
2. GuideMarker in the cue row (line 593-597) aligns visually with the lyric row — same `getGuideColumn(lineIndex, activeInstrument)` is used for both
3. Both use monospace font — cue row (line 163: `font-mono`), lyric row (line 616: `font-mono`)
4. Clicking lyric row updates guide column (lines 605-607 `onClick`, `onKeyUp`, `onSelect` all call `setGuideColumn`)

EDIT-03 (distinct cue styling without disabled look):
1. Cue row uses `bg-primary/5` (line 163) — visually distinct but active-looking
2. Lyric row uses `bg-background` (line 616) — clearly different from cue
3. No `disabled` attribute or muted styling on cue rows
4. Percussion block uses `bg-card/80` (line 563) with border — also distinct but active

If the implementation is correct, add a comment near the cue row label:
`// Phase 6: EDIT-02 (linked monospace alignment via guideColumn) and EDIT-03 (distinct active styling) verified`

If styling needs adjustment to be more visually distinct per EDIT-03 ("clearly distinguishable from lyrics through typography and/or color treatment"), update cue row styling to be more clearly playable cues rather than just a slightly tinted input.
</action>
  <verify>npm run typecheck passes; visual verification: cue rows and lyric rows are visually distinct but both look active (not disabled)</verify>
  <done>Cue rows are visually distinct from lyrics without appearing disabled, with linked monospace alignment</done>
</task>

<task type="auto">
  <name>Task 3: Verify copy-to-all and copy-to-one actions exist per line (CUE-05)</name>
  <files>src/components/SheetEditor.tsx</files>
  <action>
Read the action button section in SheetEditor.tsx (lines 516-539) and verify:

CUE-05 requirements: "Triggering copy-to-all duplicates the selected instrument cue into every other supported instrument; triggering copy-to-one duplicates only to the chosen target"

1. Copy-to-all button exists at line 517: `<Button variant="ghost" size="sm" onClick={() => handleCopyLine(lineIndex, 'all')}>`
2. Copy-to-one buttons exist at lines 521-529: filtered for instruments not equal to activeInstrument, each calls `handleCopyLine(lineIndex, instrument)`
3. `handleCopyLine` implementation at lines 361-368 correctly routes to `copyLineCuesToAll` for 'all' and `copyLineCues` for specific targets

Per D-08: "Percussion copy treats the full 5-lane cue set as one bundled unit" — this is handled by `flattenDrumCues` in lyrics-utils.ts when copying from drums to piano/guitar, and by filling all 5 lanes when copying from piano/guitar to drums.

If all correct, add a comment near the copy buttons:
`// Phase 6: CUE-05 copy-to-all and copy-to-one verified per lyric line`

Also verify that instrument-level copy behavior respects D-08: when source is drums, the `flattenDrumCues` function at lyrics-utils.ts line 155-163 converts 5-lane to single string for non-drum targets.
</action>
  <verify>npm run typecheck passes; verify copyLineCuesToAll correctly fills both piano and guitar (and drums via 5-lane fill) when called</verify>
  <done>Per-line copy-to-all and copy-to-one actions exist and correctly copy instrument cue data</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| {user → SheetEditor} | Cue input crosses here on every keystroke |
| {SheetEditor → SheetContext} | State update crosses here on blur/change |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03 | Spoofing | Copy action targets | mitigate | Copy buttons are inline (not menus) per D-09 — user sees exactly which target will receive cues |
| T-06-04 | Tamper | Cue input via JSON import | mitigate | Zod validation already in Plan 01; editor receives pre-validated data only |
</threat_model>

<verification>
- `npm run typecheck` passes
- `npm run build` succeeds
- CueRowInput stable caret verified via code review
- Linked monospace alignment verified via code review  
- Copy actions exist and route to correct utility functions
- No disabled styling on cue rows
</verification>

<success_criteria>
- Caret stays stable when typing multi-character chords (EDIT-01)
- Cue and lyric rows share same monospace alignment grid with guideColumn tracking (EDIT-02)
- Cue rows visually distinct from lyrics without disabled appearance (EDIT-03)
- Copy-to-all button exists for each line (CUE-05)
- Copy-to-one buttons exist for each other instrument per line (CUE-05)
- Percussion copy bundles 5 lanes as one unit (D-08 via flattenDrumCues)
</success_criteria>

<output>
After completion, create `.planning/phases/06-instrument-specific-cues-editor-precision/06-02-SUMMARY.md`
</output>