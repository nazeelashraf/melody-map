---
phase: 08-shell-library-redesign
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/layout/AppLayout.tsx
  - src/components/layout/Sidebar.tsx
  - src/components/layout/TopBar.tsx
autonomous: true
requirements:
  - SHEL-01
  - RESP-01
must_haves:
  truths:
    - User sees a clear warm-canvas/dark-shell split in the app layout
    - Desktop sidebar is dark shell with navigation, main content is warm canvas
    - Mobile navigation is accessible via drawer with hamburger menu
    - Top bar shows context-aware title and dark-mode toggle
    - Shell layout works across mobile, tablet, and desktop breakpoints
  artifacts:
    - path: src/components/layout/AppLayout.tsx
      provides: Shell layout with canvas/shell zoning and responsive behavior
      min_lines: 60
    - path: src/components/layout/Sidebar.tsx
      provides: Dark shell navigation sidebar with sheet/composition links and create actions
      min_lines: 100
    - path: src/components/layout/TopBar.tsx
      provides: Context-aware top bar with mobile menu toggle and theme switch
      min_lines: 50
  key_links:
    - from: AppLayout.tsx
      to: Sidebar.tsx
      via: component composition
      pattern: "<Sidebar"
    - from: AppLayout.tsx
      to: TopBar.tsx
      via: component composition
      pattern: "<TopBar"
    - from: AppLayout.tsx
      to: mobile Sheet drawer
      via: shadcn Sheet component
      pattern: "<Sheet.*side=\"left\""
    - from: Sidebar.tsx
      to: React Router
      via: Link components
      pattern: "from.*react-router-dom"
---

<objective>
Redesign the app shell (AppLayout, Sidebar, TopBar) to create an intentional warm-canvas / dark-shell visual split with cohesive navigation and responsive behavior.

Purpose: Establish the structural foundation that all other redesigned surfaces depend on. The shell must clearly separate navigation (dark) from content (warm canvas) and work across all screen sizes.
Output: Redesigned AppLayout, Sidebar, and TopBar components using Phase 7 design tokens.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

## Current Shell Implementation

From src/components/layout/AppLayout.tsx:
- Uses flex layout with sidebar (w-64, hidden on mobile) and main content area
- Mobile sidebar via shadcn Sheet drawer (side="left")
- TopBar receives page title derived from route + sheet/composition state
- Main content has overflow-y-auto with padding

From src/components/layout/Sidebar.tsx:
- Dark background (`bg-shell`) with sheet/composition navigation lists
- Shows item counts via Badge
- Active state uses `bg-accent-muted text-accent`
- Bottom section has "New Sheet" link
- Uses React Router Link for navigation

From src/components/layout/TopBar.tsx:
- Shows mobile hamburger menu (md:hidden)
- Context-aware title from AppLayout
- Dark mode toggle button (Sun/Moon icons)

## Design Tokens Available (Phase 7)

From src/index.css:
- `--color-canvas: #f6f5f4` (light) / `#1c1c1e` (dark)
- `--color-canvas-foreground`, `--color-canvas-muted`
- `--color-shell: #0f1011`
- `--color-shell-foreground: #e5e5e5`
- `--color-shell-surface: #1a1b1c`
- `--color-shell-border: rgba(255, 255, 255, 0.08)`
- `--color-accent: #5e6ad2` (light) / `#7170ff` (dark)
- `--color-accent-muted: rgba(94, 106, 210, 0.12)` (light) / `rgba(113, 112, 255, 0.18)` (dark)
- Focus: solid violet outline, Selection: dashed violet border
- Instrument colors: piano (blue), guitar (green), drums (purple)

## Key Constraints

- Preserve all existing navigation flows (library, sheets, compositions)
- Preserve dark mode toggle functionality
- Mobile sidebar must remain accessible via hamburger
- No changes to routing or state management
- Use Tailwind classes with design tokens, no inline styles
</context>

<interfaces>
<!-- Key types and contracts for the executor -->

From src/context/SheetContext.tsx ( Sheet state ):
```typescript
interface SheetState {
  sheets: Sheet[];
}
interface Sheet {
  id: string;
  title: string;
  tempo: number;
  lyricsLines: LyricLine[];
  arrangements: { piano: string; guitar: string; drums: string };
}
```

From src/context/CompositionContext.tsx ( Composition state ):
```typescript
interface CompositionState {
  compositions: Composition[];
}
interface Composition {
  id: string;
  title: string;
  sheetIds: string[];
}
```

From src/context/ThemeProvider.tsx:
```typescript
type Theme = 'light' | 'dark';
function useTheme(): { theme: Theme; setTheme: (t: Theme) => void }
```

From src/components/layout/AppLayout.tsx:
```typescript
export default function AppLayout({ children }: { children: React.ReactNode })
```

From src/components/layout/Sidebar.tsx:
```typescript
interface SidebarProps {
  collapsed?: boolean;
  onNavigate?: () => void;
}
```

From src/components/layout/TopBar.tsx:
```typescript
interface TopBarProps {
  onMenuClick?: () => void;
  title?: string;
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Redesign Sidebar with dark shell styling and improved navigation</name>
  <files>src/components/layout/Sidebar.tsx</files>
  <action>
Redesign the Sidebar component to enhance the dark shell visual identity and improve navigation clarity.

Requirements per SHEL-01 and RESP-01:
1. **Visual hierarchy**: 
   - App branding area at top with Music icon and "Melody Map" text — use larger font size (text-lg) and accent color for icon
   - Clear section separation between Sheets and Compositions with visual dividers
   - Section headers should use uppercase tracking-wide text-xs with shell-foreground/50 color

2. **Navigation items**:
   - Each nav item should have clear hover state using `hover:bg-shell-surface` and `hover:text-shell-foreground`
   - Active state should use `bg-accent-muted text-accent` with a subtle left border accent (border-l-2 border-accent) for stronger selection indication
   - Increase padding on nav items (py-2 instead of py-1.5) for better touch targets
   - Add transition-colors for smooth state changes

3. **Counts and metadata**:
   - Badge should be smaller (text-xs) and use `bg-shell-surface text-shell-foreground/70`
   - Sheet/composition titles should truncate with max-width

4. **Bottom actions**:
   - "New Sheet" button should be more prominent — use a button-like appearance with `bg-shell-surface hover:bg-accent-muted` and accent text
   - Add "New Composition" quick action as well
   - Group create actions with clear icons

5. **Scroll behavior**:
   - Ensure nav area scrolls independently if content overflows
   - Keep bottom actions pinned at bottom

6. **Accessibility**:
   - Maintain aria-labels and keyboard navigation
   - Focus states should show solid violet outline (already in CSS)

Do NOT change the component interface (SidebarProps), routing logic, or state imports. Do NOT add new dependencies.
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>
    - Sidebar renders with enhanced dark shell styling
    - Active nav items have left accent border + muted background
    - Hover states are visible and smooth
    - Bottom create actions are prominent and styled
    - TypeScript compiles without errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Redesign TopBar with context-aware title and improved actions</name>
  <files>src/components/layout/TopBar.tsx</files>
  <action>
Redesign the TopBar component to better fit the warm-canvas / dark-shell split and provide clearer context.

Requirements per SHEL-01:
1. **Visual treatment**:
   - Background should be `bg-shell` (dark) to match sidebar, creating a full-width dark shell band at the top
   - Bottom border using `border-shell-border` for subtle separation
   - Increase height slightly (py-3 instead of py-2) for better visual presence

2. **Title treatment**:
   - Page title should use `text-shell-foreground` with `font-semibold text-lg`
   - Add a subtle breadcrumb or context hint for non-library pages (e.g., "Library > Sheet Name" pattern or just the sheet/composition name)
   - On mobile, title should remain visible and not wrap awkwardly

3. **Actions**:
   - Dark mode toggle should remain but use `hover:bg-shell-surface` for consistency
   - Ensure button has proper focus ring
   - Consider adding a subtle label "Theme" on desktop (hidden on mobile) for clarity, or keep icon-only

4. **Mobile menu button**:
   - Hamburger should be clearly visible against dark background
   - Use `text-shell-foreground` with `hover:bg-shell-surface`
   - Ensure touch target is at least 44x44px

5. **Responsive**:
   - On desktop: title aligned left, actions aligned right
   - On mobile: hamburger + title, actions remain accessible

Do NOT change the TopBarProps interface. Do NOT modify the theme toggle logic.
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>
    - TopBar has dark shell background matching sidebar
    - Title is clearly visible and context-aware
    - Dark mode toggle is styled consistently
    - Mobile hamburger is accessible
    - TypeScript compiles without errors
  </done>
</task>

<task type="auto">
  <name>Task 3: Redesign AppLayout with intentional canvas/shell zoning</name>
  <files>src/components/layout/AppLayout.tsx</files>
  <action>
Redesign the AppLayout component to create a more intentional warm-canvas / dark-shell visual split.

Requirements per SHEL-01 and RESP-01:
1. **Canvas/shell zoning**:
   - Sidebar and TopBar should both use `bg-shell` creating a cohesive dark shell zone
   - Main content area must use `bg-canvas` creating a clear warm canvas zone
   - The contrast between shell and canvas should be immediately obvious

2. **Layout structure**:
   - Keep the flex row layout (sidebar + content column)
   - Sidebar width can remain w-64 on desktop
   - Ensure the layout fills the viewport height (h-full on root elements)

3. **Main content area**:
   - Use `bg-canvas` background
   - Padding should be generous: p-6 on desktop, p-4 on mobile
   - Remove any border between TopBar and main content — let the color contrast provide separation
   - Main content should have `overflow-y-auto` for independent scrolling

4. **Mobile behavior**:
   - Mobile sidebar drawer (Sheet component) should have dark background (`bg-shell`)
   - Sheet content should fill the drawer without extra padding
   - Ensure mobile drawer closes on navigation

5. **Responsive breakpoints**:
   - Desktop (md+): sidebar visible, content has full width
   - Tablet/mobile: sidebar hidden, hamburger reveals drawer
   - Test that layout doesn't break at intermediate widths

6. **Page title logic**:
   - Keep existing getPageTitle logic that derives title from route and state
   - Ensure title updates when navigating between routes

Do NOT change the AppLayout props interface. Do NOT modify routing. Do NOT add new state imports.
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>
    - AppLayout creates clear warm-canvas / dark-shell split
    - Desktop: sidebar + topbar are dark shell, main content is warm canvas
    - Mobile: hamburger opens dark shell drawer
    - Layout fills viewport and scrolls independently
    - TypeScript compiles without errors
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→localStorage | Theme preference persisted locally |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Information Disclosure | TopBar title | accept | Sheet/composition titles are already visible in sidebar; no PII exposure |
| T-08-02 | Denial of Service | Mobile drawer | mitigate | Ensure drawer can be closed via overlay click, back button, or close button |
</threat_model>

<verification>
- `npm run typecheck` passes
- `npm run build` passes
- Visual verification: Desktop shows clear dark shell / warm canvas split
- Visual verification: Mobile hamburger opens sidebar drawer
- Visual verification: Navigation between routes updates title correctly
</verification>

<success_criteria>
1. AppLayout creates an obvious warm-canvas / dark-shell visual split
2. Sidebar has enhanced dark shell styling with clear active/hover states
3. TopBar is dark shell with context-aware title
4. Mobile navigation works via hamburger → drawer
5. All existing navigation flows preserved (library, sheet, composition routes)
6. No TypeScript or build errors
</success_criteria>

<output>
After completion, create `.planning/phases/08-shell-library-redesign/08-01-SUMMARY.md`
</output>
