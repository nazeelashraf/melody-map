---
phase: 08-shell-library-redesign
plan: 02
type: execute
wave: 2
depends_on:
  - 08-01-PLAN.md
files_modified:
  - src/components/ConfirmDialog.tsx
  - src/components/ImportDialog.tsx
  - src/components/EmptyState.tsx
autonomous: true
requirements:
  - UI-01
must_haves:
  truths:
    - Confirm dialogs share the same visual language with clear destructive actions
    - Import dialog uses cohesive card/surface styling with clear file selection
    - Empty states use the warm canvas background with inviting iconography and clear CTAs
    - All dialogs have consistent padding, borders, and typography
  artifacts:
    - path: src/components/ConfirmDialog.tsx
      provides: Cohesive confirmation dialog with clear cancel/confirm actions
      min_lines: 40
    - path: src/components/ImportDialog.tsx
      provides: Cohesive import dialog with file selection and error display
      min_lines: 90
    - path: src/components/EmptyState.tsx
      provides: Cohesive empty state with canvas styling and CTA
      min_lines: 35
  key_links:
    - from: ConfirmDialog.tsx
      to: SheetCard.tsx / CompositionCard.tsx
      via: onConfirm/onCancel callbacks
      pattern: "onConfirm.*onCancel"
    - from: ImportDialog.tsx
      to: SheetList.tsx
      via: mode/onClose props
      pattern: "mode=.*onClose"
    - from: EmptyState.tsx
      to: SheetList.tsx
      via: variant/actionLabel/onAction props
      pattern: "variant=.*actionLabel"
---

<objective>
Redesign shared UI primitives (ConfirmDialog, ImportDialog, EmptyState) to establish a cohesive visual language that all library and editor components will use.

Purpose: Dialogs and empty states are the most frequently seen interactive surfaces. They must feel consistent with the warm-canvas / dark-shell design system and use the same spacing, typography, and color language.
Output: Redesigned ConfirmDialog, ImportDialog, and EmptyState using Phase 7 design tokens.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

## Current Dialog/EmptyState Implementation

From src/components/ConfirmDialog.tsx:
- Uses shadcn AlertDialog components
- Simple layout: title + cancel/delete buttons
- Destructive action uses `bg-destructive` class
- No icon or additional context

From src/components/ImportDialog.tsx:
- Uses shadcn Dialog components
- File picker button + hidden file input
- Error message in red text
- Cancel button in footer
- Title: "Import Sheet" or "Import Composition"

From src/components/EmptyState.tsx:
- Uses Inbox icon from lucide-react
- Dashed border container (`border-2 border-dashed border-border`)
- Muted background (`bg-muted/50`)
- Optional title, description, action button

## Design Tokens Available (Phase 7)

- `--color-canvas: #f6f5f4` (light) / `#1c1c1e` (dark)
- `--color-canvas-muted: #e5e5e5` (light) / `#2c2c2e` (dark)
- `--color-card: #ffffff` (light) / `#252627` (dark)
- `--color-card-foreground: #1a1b1c` (light) / `#e5e5e5` (dark)
- `--color-accent: #5e6ad2` (light) / `#7170ff` (dark)
- `--color-accent-muted: rgba(94, 106, 210, 0.12)` (light) / `rgba(113, 112, 255, 0.18)` (dark)
- `--color-muted-foreground: #6b7280` (light) / `#9ca3af` (dark)
- `--color-border: #e5e5e5` (light) / `rgba(255, 255, 255, 0.1)` (dark)
- Focus: solid violet outline, Selection: dashed violet border
- Border radius: `--radius: 0.5rem`

## Key Constraints

- Preserve all existing props interfaces — these are consumed by SheetCard, CompositionCard, SheetList
- ConfirmDialog must remain a blocking confirmation with cancel/confirm actions
- ImportDialog must preserve file upload, JSON validation, and error display behavior
- EmptyState must remain usable with/without action button
- Use shadcn components (Dialog, AlertDialog, Button) — do not replace with custom implementations
</context>

<interfaces>
<!-- Key types and contracts for the executor -->

From src/components/ConfirmDialog.tsx:
```typescript
interface ConfirmDialogProps {
  message: string;
  confirmLabel?: string;
  onConfirm: () => void;
  onCancel: () => void;
}
```

From src/components/ImportDialog.tsx:
```typescript
interface ImportDialogProps {
  mode: 'sheet' | 'composition';
  onClose: () => void;
}
```

From src/components/EmptyState.tsx:
```typescript
interface EmptyStateProps {
  title: string;
  description?: string;
  actionLabel?: string;
  onAction?: () => void;
  variant?: 'primary' | 'secondary';
}
```

From src/schemas/sheet.schema.ts:
```typescript
export const sheetSchema: z.ZodSchema<Sheet>;
export const compositionSchema: z.ZodSchema<Composition>;
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Redesign ConfirmDialog with cohesive visual language</name>
  <files>src/components/ConfirmDialog.tsx</files>
  <action>
Redesign the ConfirmDialog to use the new design system while preserving its blocking confirmation behavior.

Requirements per UI-01:
1. **Visual presence**:
   - Add an alert/warning icon (AlertTriangle from lucide-react) above the title in accent color to draw attention
   - Dialog should feel centered and modal with proper overlay
   - Use `bg-card` for dialog background and `text-card-foreground` for text

2. **Title and message**:
   - Title should use `text-lg font-semibold text-foreground`
   - Message should use `text-sm text-muted-foreground` for secondary information
   - If message is long, ensure it wraps gracefully

3. **Action buttons**:
   - Cancel button: use `variant="outline"` with subtle border
   - Confirm/Delete button: keep `bg-destructive text-destructive-foreground` but add hover state `hover:bg-destructive/90`
   - Add proper spacing between buttons (gap-3)
   - Buttons should be right-aligned in footer

4. **Spacing and sizing**:
   - Increase padding inside dialog (`p-6`)
   - Ensure dialog doesn't feel cramped on mobile
   - Max-width should be reasonable (`sm:max-w-md`)

5. **Accessibility**:
   - Maintain AlertDialog semantics (role="alertdialog")
   - Ensure focus trap works correctly
   - Focus should land on the cancel button by default (less destructive action)
   - Escape key should trigger cancel

Do NOT change the props interface. Do NOT change the onConfirm/onCancel behavior. Do NOT remove the AlertDialog wrapper.
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>
    - ConfirmDialog has alert icon and improved visual hierarchy
    - Cancel and confirm buttons are clearly styled and spaced
    - Dialog uses card background and proper padding
    - Focus management and keyboard navigation work correctly
    - TypeScript compiles without errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Redesign ImportDialog with cohesive file upload experience</name>
  <files>src/components/ImportDialog.tsx</files>
  <action>
Redesign the ImportDialog to provide a more polished file upload experience consistent with the design system.

Requirements per UI-01:
1. **Visual layout**:
   - Use `bg-card` background for dialog content
   - Title should use `text-lg font-semibold text-foreground`
   - Add a file upload icon (FileUp from lucide-react) in the header or as a visual accent

2. **File selection area**:
   - Replace the simple button with a larger drag-and-drop style area (even if drag-and-drop isn't implemented, the visual affordance helps)
   - Use a dashed border box (`border-2 border-dashed border-border rounded-lg`) with `bg-muted/50`
   - Center the upload icon and "Choose File" text inside the box
   - Make the entire area clickable (wrap the button and hidden input)
   - Add helper text: "Select a JSON file to import" in `text-sm text-muted-foreground`

3. **Error display**:
   - Error message should use `text-destructive text-sm` with an alert icon
   - Add padding around error message for visibility
   - Error should appear below the file area, not cramped against it

4. **Footer actions**:
   - Cancel button should use `variant="outline"`
   - Right-align footer actions
   - Ensure proper spacing (`gap-3`)

5. **File input handling**:
   - Keep the existing file reading and Zod validation logic exactly as-is
   - Preserve the crypto.randomUUID() assignment for imported IDs
   - Preserve error handling for JSON parse errors and validation failures

6. **Responsive**:
   - Dialog should be usable on mobile screens
   - File area should not overflow on narrow screens

Do NOT change the props interface (mode, onClose). Do NOT change the validation logic. Do NOT change the dispatch calls.
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>
    - ImportDialog has a prominent file selection area with dashed border
    - Upload icon and helper text guide the user
    - Error messages are clearly visible with alert styling
    - Footer has cancel button with proper spacing
    - File reading and validation logic is preserved
    - TypeScript compiles without errors
  </done>
</task>

<task type="auto">
  <name>Task 3: Redesign EmptyState for warm canvas context</name>
  <files>src/components/EmptyState.tsx</files>
  <action>
Redesign the EmptyState component to feel inviting and consistent with the warm-canvas design language.

Requirements per UI-01:
1. **Visual treatment**:
   - Container should use `bg-canvas` or `bg-card` (not `bg-muted/50`) to feel like a real content area, not a placeholder
   - Use `border border-border` instead of dashed border — the empty state is a valid state, not an error
   - Increase padding (`p-10` or `p-12`) to give it room to breathe
   - Use rounded corners (`rounded-xl` or `rounded-2xl`)

2. **Iconography**:
   - Keep the Inbox icon but make it larger (`h-12 w-12`)
   - Icon color should be `text-muted-foreground/40` (very subtle, not distracting)
   - Add more vertical spacing between icon and title (`mb-4`)

3. **Typography**:
   - Title: `text-xl font-semibold text-foreground`
   - Description: `text-sm text-muted-foreground mt-2 max-w-sm mx-auto` (centered, constrained width)
   - Ensure text is centered

4. **Call-to-action**:
   - Action button should be more prominent — use `variant="default"` (accent background) for primary empty states
   - Add margin-top (`mt-6`) to separate from text
   - Button should have clear label with icon (Plus from lucide-react)

5. **Variant handling**:
   - `variant="primary"`: Use accent button, full visual weight
   - `variant="secondary"`: Use outline button, slightly less visual weight
   - Both should feel cohesive with the rest of the app

6. **Animation (subtle)**:
   - Add a very subtle fade-in or scale animation on mount if easily achievable with Tailwind (`animate-in fade-in duration-500`)
   - Only if Tailwind animate plugin is available; otherwise skip

Do NOT change the props interface. Do NOT remove any props. Ensure the component still works when actionLabel/onAction are omitted.
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>
    - EmptyState uses card/canvas background with solid border
    - Icon is larger and more subtly colored
    - Typography is centered and well-spaced
    - CTA button is prominent with proper styling
    - Both variants (primary/secondary) look cohesive
    - Component still works without action props
    - TypeScript compiles without errors
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→file system | ImportDialog reads user-selected files |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-03 | Tampering | ImportDialog file upload | mitigate | Preserve existing Zod schema validation; reject unvalidated data |
| T-08-04 | Information Disclosure | ImportDialog error messages | accept | Error messages expose validation failures to the user who uploaded the file; no sensitive data |
| T-08-05 | Denial of Service | ImportDialog large files | accept | FileReader reads as text; browser handles memory limits; no server impact |
</threat_model>

<verification>
- `npm run typecheck` passes
- `npm run build` passes
- ConfirmDialog: Visual check shows alert icon, proper spacing, clear cancel/confirm buttons
- ImportDialog: Visual check shows dashed file area, upload icon, clear error display
- EmptyState: Visual check shows centered content, prominent CTA, solid border
</verification>

<success_criteria>
1. ConfirmDialog has improved visual hierarchy with alert icon and clear actions
2. ImportDialog has a prominent file selection area with dashed border affordance
3. EmptyState feels inviting with centered layout and prominent CTA
4. All three components use consistent padding, borders, and typography
5. No props interfaces changed — all consumers still compile
6. No TypeScript or build errors
</success_criteria>

<output>
After completion, create `.planning/phases/08-shell-library-redesign/08-02-SUMMARY.md`
</output>
