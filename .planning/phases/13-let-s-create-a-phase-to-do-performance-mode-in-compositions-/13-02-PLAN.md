---
phase: 13-let-s-create-a-phase-to-do-performance-mode-in-compositions
plan: 02
type: execute
wave: 2
depends_on:
  - 13-01
files_modified:
  - src/components/CompositionEditor.tsx
  - src/components/CompositionPerformanceView.tsx
  - src/styles/print.css
autonomous: true
requirements:
  - CPERF-03
  - CPERF-04
must_haves:
  truths:
    - User can enter composition performance mode from the composition editor with a visible text action.
    - Each sheet stays visually separated with section index, title, tempo, and an inline missing-sheet warning when needed.
    - Printing composition performance mode preserves composition metadata, active instrument context, clear section boundaries, and an empty-state surface for empty compositions.
  artifacts:
    - path: src/components/CompositionEditor.tsx
      provides: Entry action from composition editing into the dedicated performance route.
    - path: src/components/CompositionPerformanceView.tsx
      provides: Section wrappers, empty state, missing-sheet block, and print metadata for collated performance mode.
    - path: src/styles/print.css
      provides: Print rules for composition section boundaries.
      contains: print-sheet-section
  key_links:
    - from: src/components/CompositionEditor.tsx
      to: /composition/:id/performance
      via: visible Performance mode link/button
      pattern: Performance mode
    - from: src/components/CompositionPerformanceView.tsx
      to: src/styles/print.css
      via: print-sheet-section class and print metadata classes
      pattern: print-sheet-section
---

<objective>
Finish the composition performance experience with route entry, section-boundary clarity, missing-sheet handling, empty-state behavior, and print completeness.

Purpose: Phase 13 is only useful if the user can reach the new route easily and the collated output stays readable on both screen and paper.
Output: Composition editor entry affordance, polished composition performance sections, and print-aware composition separators.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-let-s-create-a-phase-to-do-performance-mode-in-compositions-/13-RESEARCH.md
@.planning/phases/13-let-s-create-a-phase-to-do-performance-mode-in-compositions-/13-UI-SPEC.md
@src/components/CompositionEditor.tsx
@src/components/CompositionPerformanceView.tsx
@src/styles/print.css

<interfaces>
From src/components/CompositionEditor.tsx:
```typescript
export default function CompositionEditor({ compositionId }: CompositionEditorProps)
```

From src/components/CompositionPerformanceView.tsx after Plan 13-01:
```typescript
export default function CompositionPerformanceView({ compositionId }: CompositionPerformanceViewProps)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add composition-editor entry into performance mode</name>
  <files>src/components/CompositionEditor.tsx</files>
  <read_first>src/components/CompositionEditor.tsx, .planning/phases/13-let-s-create-a-phase-to-do-performance-mode-in-compositions-/13-UI-SPEC.md, src/components/SheetEditor.tsx</read_first>
  <action>In `src/components/CompositionEditor.tsx`, add one visible text action labeled exactly `Performance mode` in the header action area near the export control, linking to `/composition/${composition.id}/performance`. Reuse the existing shadcn `Button` language and keep the action visible as text rather than icon-only, per the accessibility and copywriting contract. Do not replace the existing export behavior, and do not add inline reader mode back into `CompositionEditor`; this task is only the route-entry affordance.</action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/CompositionEditor.tsx` contains the text `Performance mode`.
    - `src/components/CompositionEditor.tsx` contains `/composition/${composition.id}/performance` or an equivalent template-literal link to that route.
    - `src/components/CompositionEditor.tsx` continues to contain `Export composition`.
  </acceptance_criteria>
  <done>The composition editor exposes a clear route-level entry into composition performance mode without removing existing edit actions.</done>
</task>

<task type="auto">
  <name>Task 2: Polish section boundaries, empty states, missing-sheet slots, and composition print behavior</name>
  <files>src/components/CompositionPerformanceView.tsx, src/styles/print.css</files>
  <read_first>src/components/CompositionPerformanceView.tsx, src/styles/print.css, .planning/phases/13-let-s-create-a-phase-to-do-performance-mode-in-compositions-/13-UI-SPEC.md, src/components/PerformanceView.tsx</read_first>
  <action>Update `src/components/CompositionPerformanceView.tsx` so each composition entry is wrapped in a structural section with a visible boundary showing the exact section eyebrow `Sheet {index}`, the sheet title, and the tempo label. Preserve order slots when a sheet is missing by rendering the inline warning copy exactly as specified: heading `Missing sheet reference` and body `This slot is still part of the composition order, but its source sheet is no longer in the library.` For empty compositions, render the dedicated empty-state copy `No sheets in this composition yet.` and `Add sheets in the composition editor to build a performance set.` inside the performance surface rather than redirecting. Keep the print-only header showing the composition title, total entry count, and active instrument label. In `src/styles/print.css`, ensure composition sections use `.print-sheet-section` with page-break avoidance and visible separator styling so section boundaries remain obvious on paper; do not introduce broad print-hiding selectors beyond the existing targeted approach.</action>
  <verify>
    <automated>npm run typecheck && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/CompositionPerformanceView.tsx` contains `Sheet {entry.index + 1}` or equivalent visible section-index rendering.
    - `src/components/CompositionPerformanceView.tsx` contains `Missing sheet reference`.
    - `src/components/CompositionPerformanceView.tsx` contains `No sheets in this composition yet.`.
    - `src/components/CompositionPerformanceView.tsx` contains `Add sheets in the composition editor to build a performance set.`.
    - `src/styles/print.css` contains `.print-sheet-section`.
    - `src/components/CompositionPerformanceView.tsx` contains `print-instrument-label`.
  </acceptance_criteria>
  <done>The composition performance surface keeps every sheet slot readable on screen and in print, including empty and missing-sheet states.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| composition editor navigation → composition performance route | User navigation changes from edit mode into a read-only collated surface |
| persisted sheet references → visible missing-slot warnings | Stale or deleted sheet ids are surfaced back to the user inline |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-03 | R | `CompositionEditor` performance link | mitigate | Use a deterministic route link to `/composition/:id/performance` so entering performance mode is traceable to explicit user navigation rather than hidden state toggles. |
| T-13-04 | I | `CompositionPerformanceView` missing-sheet sections | mitigate | Render missing references as inline visible warnings without leaking any deleted sheet data beyond the missing slot status already implied by the stored composition order. |
</threat_model>

<verification>
- `npm run typecheck`
- `npm run build`
</verification>

<success_criteria>
- CompositionEditor has a visible `Performance mode` action linking to the dedicated route.
- Composition performance mode shows obvious section breaks, missing-sheet warnings, and an empty-state surface.
- Print output preserves composition title, active instrument label, and per-sheet separation.
</success_criteria>

<output>
After completion, create `.planning/phases/13-let-s-create-a-phase-to-do-performance-mode-in-compositions-/13-let-s-create-a-phase-to-do-performance-mode-in-compositions-02-SUMMARY.md`
</output>
