---
phase: 13-let-s-create-a-phase-to-do-performance-mode-in-compositions
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/performance-shared.tsx
  - src/components/PerformanceView.tsx
  - src/components/CompositionPerformanceView.tsx
  - src/App.tsx
autonomous: true
requirements:
  - CPERF-01
  - CPERF-02
must_haves:
  truths:
    - User can open a dedicated composition performance route that renders the composition as one reading-first surface.
    - User can switch a single active instrument across the whole composition performance surface.
    - Piano and guitar keep the grouped-or-mono rendering behavior already established in sheet performance mode, while percussion keeps lane rendering.
  artifacts:
    - path: src/components/performance-shared.tsx
      provides: Shared single-sheet performance rendering reused by both sheet and composition performance surfaces.
      exports:
        - PerformanceSheetContent
        - instrumentLabels
        - instrumentTabClasses
    - path: src/components/CompositionPerformanceView.tsx
      provides: Composition-level performance route component with shared instrument tabs and ordered section rendering.
      exports:
        - default
    - path: src/App.tsx
      provides: Dedicated `/composition/:id/performance` route wiring.
      contains: /composition/:id/performance
  key_links:
    - from: src/App.tsx
      to: src/components/CompositionPerformanceView.tsx
      via: route element for /composition/:id/performance
      pattern: /composition/:id/performance
    - from: src/components/CompositionPerformanceView.tsx
      to: src/components/performance-shared.tsx
      via: PerformanceSheetContent import
      pattern: PerformanceSheetContent
---

<objective>
Create the dedicated composition performance reading surface and route by reusing the existing single-sheet performance renderer.

Purpose: Phase 13 starts with a working route-level composition reader so the user can view an ordered set without re-entering edit mode for each sheet.
Output: Shared performance rendering module, updated single-sheet performance wiring, and a composition performance route component.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-let-s-create-a-phase-to-do-performance-mode-in-compositions-/13-RESEARCH.md
@.planning/phases/13-let-s-create-a-phase-to-do-performance-mode-in-compositions-/13-UI-SPEC.md
@.planning/phases/10-performance-print-redesign/10-01-SUMMARY.md
@.planning/phases/10-performance-print-redesign/10-02-SUMMARY.md
@src/App.tsx
@src/components/PerformanceView.tsx
@src/components/CompositionEditor.tsx
@src/context/CompositionContext.tsx
@src/context/SheetContext.tsx
@src/types/index.ts

<interfaces>
From src/App.tsx:
```typescript
function CompositionEditorPage() {
  const { id } = useParams<{ id: string }>();
  return <CompositionEditor compositionId={id ?? ''} />;
}
```

From src/types/index.ts:
```typescript
export type InstrumentType = 'piano' | 'guitar' | 'drums';

export interface Sheet {
  id: string;
  title: string;
  tempo: number;
  lyricsLines: LyricsLine[];
  arrangements: {
    piano: string;
    guitar: string;
    drums: string;
  };
}

export interface Composition {
  id: string;
  title: string;
  sheetIds: string[];
}
```

From src/context/CompositionContext.tsx:
```typescript
interface CompositionState {
  compositions: Composition[];
  activeCompositionId: string | null;
}

export function useCompositionActions() {
  const setActiveComposition = useCallback((id: string | null) => {
    dispatch({ type: 'SET_ACTIVE_COMPOSITION', payload: id });
  }, [dispatch]);
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract shared sheet-performance rendering for reuse</name>
  <files>src/components/performance-shared.tsx, src/components/PerformanceView.tsx</files>
  <read_first>src/components/PerformanceView.tsx, src/types/index.ts, src/lib/lyrics-utils.ts, .planning/phases/13-let-s-create-a-phase-to-do-performance-mode-in-compositions-/13-UI-SPEC.md</read_first>
  <action>Create `src/components/performance-shared.tsx` and move the reusable sheet-reading pieces out of `PerformanceView.tsx`: the `instrumentLabels` record, `instrumentTabClasses` record, grouped chord line renderer, and the body content renderer that handles piano/guitar grouped-vs-mono display, percussion lane display, and arrangement notes. Export that renderer as `PerformanceSheetContent({ sheet, instrument, viewFormat })`. Update `src/components/PerformanceView.tsx` to import and use those shared exports while preserving the existing single-sheet route behavior from Phase 10: `performance-mode-active` body class, one page-level instrument tab list, one grouped/mono toggle that hides for percussion, the `Back to edit` button, and the print metadata header. Keep the existing user-facing copy and print classes intact; this task is a refactor for reuse, not a redesign.</action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/performance-shared.tsx` contains `export function PerformanceSheetContent(`.
    - `src/components/performance-shared.tsx` contains `export const instrumentLabels`.
    - `src/components/PerformanceView.tsx` contains `from '@/components/performance-shared'`.
    - `src/components/PerformanceView.tsx` still contains `performance-mode-active`.
  </acceptance_criteria>
  <done>The existing sheet performance surface now delegates to a shared renderer that later plans can reuse without duplicating cue/lyrics formatting logic.</done>
</task>

<task type="auto">
  <name>Task 2: Add composition performance route and shared-instrument surface</name>
  <files>src/components/CompositionPerformanceView.tsx, src/App.tsx</files>
  <read_first>src/App.tsx, src/components/CompositionEditor.tsx, src/context/CompositionContext.tsx, src/context/SheetContext.tsx, src/components/performance-shared.tsx, .planning/phases/13-let-s-create-a-phase-to-do-performance-mode-in-compositions-/13-UI-SPEC.md</read_first>
  <action>Create `src/components/CompositionPerformanceView.tsx` as the dedicated reader for `/composition/:id/performance` per the UI contract. Resolve the target composition from `CompositionContext`, join `composition.sheetIds` against `sheetState.sheets` in order, and keep a single page-level `activeTab` state for `piano`, `guitar`, and `drums` plus a `viewFormat` state of `grouped | mono` that hides entirely for percussion. Reuse `PerformanceSheetContent` to render each existing sheet section. The screen header must show the composition title, the exact supporting copy `{N} sheet(s) in performance order`, and a visible `Back to edit` affordance linking to `/composition/:id`. Reuse the existing `performance-mode-active` body treatment. In `src/App.tsx`, add `CompositionPerformancePage` using `useParams<{ id: string }>()` and wire `<Route path="/composition/:id/performance" element={<CompositionPerformancePage />} />` without changing existing routes.</action>
  <verify>
    <automated>npm run typecheck && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `src/App.tsx` contains `/composition/:id/performance`.
    - `src/components/CompositionPerformanceView.tsx` contains `setActiveComposition(compositionId)`.
    - `src/components/CompositionPerformanceView.tsx` contains `sheetIds.map((sheetId, index)`.
    - `src/components/CompositionPerformanceView.tsx` contains `PerformanceSheetContent`.
    - `src/components/CompositionPerformanceView.tsx` contains `Back to edit`.
    - `src/components/CompositionPerformanceView.tsx` contains `sheet` and `sheets in performance order` copy logic.
  </acceptance_criteria>
  <done>The app exposes a dedicated composition performance route that reads live sheets in stored order and renders them under one shared instrument context.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| route params + persisted composition ids → composition performance surface | User-controlled navigation and local persisted data determine which composition and sheet entries are rendered |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01 | T | `CompositionPerformanceView` route resolution | mitigate | Resolve the composition id only through existing `CompositionContext` state and fall back to the established not-found view when the id does not match a stored composition. |
| T-13-02 | D | `CompositionPerformanceView` ordered sheet rendering | mitigate | Render composition entries from `composition.sheetIds` once per render and reuse `PerformanceSheetContent` so large collated output does not duplicate formatting logic across per-sheet components. |
</threat_model>

<verification>
- `npm run typecheck`
- `npm run build`
</verification>

<success_criteria>
- The app has a dedicated `/composition/:id/performance` route.
- Single-sheet and composition performance surfaces share one rendering implementation for cues, grouped chords, and notes.
- Composition performance mode keeps one instrument context across the entire collated view.
</success_criteria>

<output>
After completion, create `.planning/phases/13-let-s-create-a-phase-to-do-performance-mode-in-compositions-/13-let-s-create-a-phase-to-do-performance-mode-in-compositions-01-SUMMARY.md`
</output>
