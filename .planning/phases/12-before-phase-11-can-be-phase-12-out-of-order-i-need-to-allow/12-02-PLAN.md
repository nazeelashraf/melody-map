---
phase: 12-before-phase-11-can-be-phase-12-out-of-order-i-need-to-allow
plan: 02
type: execute
wave: 2
depends_on:
  - 12-01
files_modified:
  - src/components/SheetEditor.tsx
autonomous: true
requirements:
  - TRNS-01
  - TRNS-04
must_haves:
  truths:
    - User can apply semitone transposition to the currently selected piano or guitar cues from the editor.
    - Percussion does not offer chord transposition controls.
    - In-place transposition persists because the editor writes updated `lyricsLines` back through existing sheet state updates.
  artifacts:
    - path: src/components/SheetEditor.tsx
      provides: Current-instrument transpose controls wired to the batch transposition utility.
  key_links:
    - from: src/components/SheetEditor.tsx
      to: src/lib/lyrics-utils.ts
      via: transposeLyricsLinesForInstrument import and apply handler
      pattern: transposeLyricsLinesForInstrument
---

<objective>
Wire the transposition utility into the sheet editor so the user can rewrite all cues for the currently selected chorded instrument in place.

Purpose: Phase 12 is only complete once the utility is reachable from normal sheet editing flows.
Output: SheetEditor controls and apply logic for piano/guitar transposition.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-before-phase-11-can-be-phase-12-out-of-order-i-need-to-allow/12-RESEARCH.md
@.planning/phases/12-before-phase-11-can-be-phase-12-out-of-order-i-need-to-allow/12-UI-SPEC.md
@src/components/SheetEditor.tsx
@src/lib/lyrics-utils.ts

<interfaces>
From src/components/SheetEditor.tsx:
```typescript
const [editorInstrument, setEditorInstrument] = useState<InstrumentType>('piano');
const updateLyricsLines = (lyricsLines: LyricsLine[]) => {
  updateSheet({ lyricsLines });
};
```

From src/lib/lyrics-utils.ts after Plan 12-01:
```typescript
export function transposeLyricsLinesForInstrument(
  lines: LyricsLine[],
  instrument: 'piano' | 'guitar',
  semitoneDelta: number,
): LyricsLine[];
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add current-instrument transpose controls to the editor toolbar</name>
  <files>src/components/SheetEditor.tsx</files>
  <read_first>src/components/SheetEditor.tsx, src/lib/lyrics-utils.ts, src/schemas/sheet.schema.ts, .planning/phases/12-before-phase-11-can-be-phase-12-out-of-order-i-need-to-allow/12-UI-SPEC.md</read_first>
  <action>In `src/components/SheetEditor.tsx`, add local UI state for a semitone selection and render one transpose control block in the cue-toolbar area below the instrument tabs and above the lyric-line editor list, matching `12-UI-SPEC.md`. Use a compact native `<select>` with option values from `-11` through `+11`, including visible default copy `0 semitones`, and size it to the existing compact control language (36px control height, wrapping cleanly inside the muted toolbar panel). The select must expose the accessible name `Transpose {Instrument} cues by semitones`. Show this block only when `editorInstrument` is `piano` or `guitar`; the primary button text must be exactly `Apply to Piano` or `Apply to Guitar` based on the current instrument. When `editorInstrument === 'drums'`, replace the entire control block with visible helper copy `Percussion cues are not transposed.` and do not render disabled transpose inputs for percussion. Disable the apply button when the selected semitone delta is `0`.</action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/SheetEditor.tsx` contains the text `Transpose`.
    - `src/components/SheetEditor.tsx` contains the text `0 semitones`.
    - `src/components/SheetEditor.tsx` contains the text `Apply to Piano` or `Apply to Guitar`.
    - `src/components/SheetEditor.tsx` contains the text `Percussion cues are not transposed.`.
    - `src/components/SheetEditor.tsx` contains option values for `-11` and `11`.
    - `src/components/SheetEditor.tsx` contains the accessible label string `Transpose ` and ` cues by semitones` for the select.
  </acceptance_criteria>
  <done>The editor exposes a semitone-selection control for piano/guitar only and clearly blocks transpose actions for drums.</done>
</task>

<task type="auto">
  <name>Task 2: Batch-apply transposition through existing sheet updates</name>
  <files>src/components/SheetEditor.tsx</files>
  <read_first>src/components/SheetEditor.tsx, src/lib/lyrics-utils.ts, src/types/index.ts, .planning/phases/12-before-phase-11-can-be-phase-12-out-of-order-i-need-to-allow/12-UI-SPEC.md</read_first>
  <action>Import `transposeLyricsLinesForInstrument` and add an apply handler that runs only for `editorInstrument === 'piano' || editorInstrument === 'guitar'`. The handler must call `updateLyricsLines(transposeLyricsLinesForInstrument(sheet.lyricsLines, editorInstrument, selectedSemitoneDelta))` so the change edits all cue rows for the selected instrument in place and persists through the existing reducer/localStorage path. After apply, reset the selector back to `0` so repeated transposes require an explicit next choice. Follow the UI contract exactly: no confirmation modal, no toast, no persistent transpose-offset state, and no hidden percussion controls. Do not mutate `lineInstrumentOverrides`, do not add new schema fields, and do not change percussion content beyond any trailing-space padding returned by the utility.</action>
  <verify>
    <automated>npm run typecheck && npx vitest run src/lib/lyrics-utils.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/SheetEditor.tsx` contains `transposeLyricsLinesForInstrument(`.
    - `src/components/SheetEditor.tsx` contains an `editorInstrument === 'piano' || editorInstrument === 'guitar'` guard for apply behavior.
    - `src/components/SheetEditor.tsx` contains `updateLyricsLines(` in the transpose apply handler.
    - `src/components/SheetEditor.tsx` resets the selected semitone state back to `0` after apply.
    - `src/components/SheetEditor.tsx` does not introduce a toast import for transpose feedback.
  </acceptance_criteria>
  <done>Applying the control rewrites the selected chorded instrument's cues across all lyric lines in place and keeps persistence on the existing sheet update path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| editor UI action → persisted sheet state | A user-selected semitone delta causes a bulk rewrite of stored cue content |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-03 | T | `SheetEditor` transpose apply handler | mitigate | Allow bulk rewrites only through `transposeLyricsLinesForInstrument` for `piano` and `guitar`, preserving schema-valid line lengths before dispatching `UPDATE_SHEET`. |
| T-12-04 | D | `SheetEditor` transpose controls | accept | This is a local single-user browser action with bounded line counts and no remote surface; the utility tests already guard against excessive overflow growth from a single apply. |
</threat_model>

<verification>
- `npm run typecheck`
- `npx vitest run src/lib/lyrics-utils.test.ts`
</verification>

<success_criteria>
- SheetEditor offers transposition only for piano and guitar.
- Apply rewrites all lyric lines for the selected chorded instrument through existing sheet state updates.
- Drums remain excluded from chord transposition UI.
</success_criteria>

<output>
After completion, create `.planning/phases/12-before-phase-11-can-be-phase-12-out-of-order-i-need-to-allow/12-before-phase-11-can-be-phase-12-out-of-order-i-need-to-allow-02-SUMMARY.md`
</output>
