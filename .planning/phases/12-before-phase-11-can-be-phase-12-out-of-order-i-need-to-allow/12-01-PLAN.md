---
phase: 12-before-phase-11-can-be-phase-12-out-of-order-i-need-to-allow
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/lib/lyrics-utils.ts
  - src/lib/lyrics-utils.test.ts
autonomous: true
requirements:
  - TRNS-01
  - TRNS-02
  - TRNS-03
must_haves:
  truths:
    - User can transpose piano or guitar cues by semitone steps.
    - Recognized chord tokens keep their original lyric start columns after transposition.
    - Longer transposed chords extend line length instead of truncating cue text.
  artifacts:
    - path: src/lib/lyrics-utils.ts
      provides: Transposition and overflow-padding utilities for cue lines.
      exports:
        - transposeChordToken
        - transposeCueLinePreservingColumns
        - transposeLyricsLinesForInstrument
    - path: src/lib/lyrics-utils.test.ts
      provides: Automated coverage for token parsing, semitone shifting, passthrough, and padding.
  key_links:
    - from: src/lib/lyrics-utils.ts
      to: src/lib/lyrics-utils.test.ts
      via: vitest utility cases
      pattern: transposeLyricsLinesForInstrument
---

<objective>
Create the transposition utility layer that can rewrite chorded cue lines in place while preserving cue anchors and extending line length safely.

Purpose: Phase 12 depends on deterministic transformation behavior before any editor control can call it.
Output: Tested utility exports for chord-token transposition and line-level overflow padding.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-before-phase-11-can-be-phase-12-out-of-order-i-need-to-allow/12-RESEARCH.md
@src/lib/lyrics-utils.ts
@src/lib/lyrics-utils.test.ts
@src/types/index.ts

<interfaces>
From src/types/index.ts:
```typescript
export type InstrumentType = 'piano' | 'guitar' | 'drums';

export interface LyricsLine {
  lyrics: string;
  cues: {
    piano: string;
    guitar: string;
    drums: DrumCues;
  };
}
```

From src/lib/lyrics-utils.ts:
```typescript
export function normalizeCueLine(rawValue: string, targetLength: number): string;
export function normalizeDrumCues(rawValue: Partial<DrumCues>, targetLength: number): DrumCues;
export function deriveGroupedChords(cueLine: string, lyrics: string): GroupedChord[];
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing tests for token transposition and overflow padding</name>
  <files>src/lib/lyrics-utils.test.ts</files>
  <read_first>src/lib/lyrics-utils.test.ts, src/lib/lyrics-utils.ts, src/types/index.ts</read_first>
  <behavior>
    - Test 1: `transposeChordToken('A', 2)` returns `B`.
    - Test 2: `transposeChordToken('Bm', 1)` returns `Cm` while preserving the `m` suffix.
    - Test 3: `transposeChordToken('Hsus4', 3)` returns `Hsus4` unchanged because the root is unrecognized.
    - Test 4: Transposing a line whose last token grows (`Am` -> `A#m`) pads `lyrics` and every cue field to the new line length instead of truncating the token.
    - Test 5: Batch transposition updates only the selected chorded instrument's note content; non-selected cues and drums only gain trailing spaces when line length increases.
  </behavior>
  <action>Create RED coverage in `src/lib/lyrics-utils.test.ts` for three new exports: `transposeChordToken`, `transposeCueLinePreservingColumns`, and `transposeLyricsLinesForInstrument`. Use the exact sharp-based state machine `A,A#,B,C,C#,D,D#,E,F,F#,G,G#`. Treat each contiguous non-space token as a candidate chord, preserve the untouched suffix after the recognized root, and assert that unknown roots remain unchanged. Add one line-overflow case where a trailing `Am` becomes `A#m` so the next implementation must extend `lyrics.length` and pad every cue field rather than slice the cue.</action>
  <verify>
    <automated>npx vitest run src/lib/lyrics-utils.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/lyrics-utils.test.ts` contains `describe('transposeChordToken'`.
    - `src/lib/lyrics-utils.test.ts` contains `describe('transposeLyricsLinesForInstrument'`.
    - `src/lib/lyrics-utils.test.ts` contains the phrase `pads lyrics when a transposed chord grows at the line end`.
  </acceptance_criteria>
  <done>Failing tests exist for semitone shifting, suffix preservation, passthrough of unknown tokens, and line-padding overflow behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement transposition utilities with fixed-column output</name>
  <files>src/lib/lyrics-utils.ts, src/lib/lyrics-utils.test.ts</files>
  <read_first>src/lib/lyrics-utils.ts, src/lib/lyrics-utils.test.ts, src/types/index.ts, src/schemas/sheet.schema.ts</read_first>
  <behavior>
    - Recognized roots transpose by modulo-12 semitone steps.
    - Token suffixes remain byte-for-byte unchanged after the root.
    - Unknown tokens are returned exactly as entered.
    - Token start indices do not move even when the replacement token is longer.
    - Expanded lines pad `lyrics`, other chorded cues, and all drum lanes to the new shared length.
  </behavior>
  <action>In `src/lib/lyrics-utils.ts`, export `transposeChordToken(token: string, semitoneDelta: number)`, `transposeCueLinePreservingColumns(cueLine: string, semitoneDelta: number)`, and `transposeLyricsLinesForInstrument(lines: LyricsLine[], instrument: 'piano' | 'guitar', semitoneDelta: number)`. Implement them with a predefined sharp-note lookup table, modulo-12 step arithmetic, and root parsing that matches only these prefixes: `A#`, `C#`, `D#`, `F#`, `G#`, `A`, `B`, `C`, `D`, `E`, `F`, `G`. Preserve the remainder of the token unchanged. Rebuild each cue line from original token start indices, compute the farthest token end, and if that end exceeds `lyrics.length`, pad `lyrics`, the non-selected chorded cue, and every drum lane with trailing spaces to the new target length before returning the updated `LyricsLine[]`. Do not introduce a persistent transpose offset or any data-model changes.</action>
  <verify>
    <automated>npx vitest run src/lib/lyrics-utils.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/lyrics-utils.ts` contains `export function transposeChordToken(`.
    - `src/lib/lyrics-utils.ts` contains `export function transposeCueLinePreservingColumns(`.
    - `src/lib/lyrics-utils.ts` contains `export function transposeLyricsLinesForInstrument(`.
    - `src/lib/lyrics-utils.ts` contains the literal sequence `'A#', 'B', 'C', 'C#'` inside the transposition lookup.
  </acceptance_criteria>
  <done>The utility module can transpose piano/guitar cue tokens, preserve token anchors, expand line length safely, and passes the focused Vitest suite.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user-entered cue text → transpose utilities | Untrusted free-form cue tokens are rewritten into persisted sheet content |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | T | `transposeChordToken` | mitigate | Match only the explicit sharp-based root table and return unknown tokens unchanged so malformed tokens are not rewritten into invalid note names. |
| T-12-02 | D | `transposeCueLinePreservingColumns` | mitigate | Iterate once over normalized cue strings and compute overflow from token end positions only; tests cover trailing-growth cases to prevent runaway padding or truncation loops. |
</threat_model>

<verification>
- `npx vitest run src/lib/lyrics-utils.test.ts`
</verification>

<success_criteria>
- Utility tests cover recognized roots, unknown tokens, wraparound, and end-of-line overflow.
- `transposeLyricsLinesForInstrument` returns schema-safe line lengths for every cue field.
- No token start index changes during transposition.
</success_criteria>

<output>
After completion, create `.planning/phases/12-before-phase-11-can-be-phase-12-out-of-order-i-need-to-allow/12-before-phase-11-can-be-phase-12-out-of-order-i-need-to-allow-01-SUMMARY.md`
</output>
